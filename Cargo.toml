[workspace]
members = [
    "netzer-core",
    "netzer-socket",
    "netzer-cli"
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["Nehonix Team"]

[workspace.dependencies]
# We will define shared dependencies here later
